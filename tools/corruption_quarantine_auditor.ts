import { emit, loadJson, scan } from './_runtime.js';
export function run(): void { const entries = scan('quarantine').map((p) => loadJson(p) as any); const reasons = new Set(['TEST_FIXTURE','HASH_MISMATCH','CORRUPTION']); const violations: string[] = []; for (const e of entries) { if (!e.authorized) violations.push(`UNAUTHORIZED:${e.quarantine_id}`); if (!e.lineage_id || !e.hash_chain_reference) violations.push(`REFERENCE:${e.quarantine_id}`); if (!reasons.has(e.reason_code)) violations.push(`REASON:${e.quarantine_id}`); if (Number.isNaN(Date.parse(e.timestamp))) violations.push(`TIMESTAMP:${e.quarantine_id}`); if (e.artifact_state === 'active' || e.pipeline_output === true) violations.push(`ACTIVE_OUTPUT:${e.quarantine_id}`); } emit({ corruption_quarantine: { status: violations.length ? 'FAIL' : 'PASS', entries: entries.length, violations } }, violations.length > 0); }
if (process.argv[1]?.endsWith('corruption_quarantine_auditor.ts')) run();
